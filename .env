MASTER_LISTEN_ADDR=127.0.0.1:5000
MASTER_ADDR=127.0.0.1:5000 # used for workers
WORKER_LISTEN_ADDR=127.0.0.1:5001 # each worker needs to have a separate address
REGISTER_TIMEOUT_SEC=5 # used for workers
